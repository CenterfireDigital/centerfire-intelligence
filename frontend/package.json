{
  "name": "centerfire-health-monitor",
  "version": "1.0.0",
  "description": "Quick health monitoring dashboard for Centerfire Intelligence agents and services",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "npm --prefix web run build",
    "build:web": "npm --prefix web run build",
    "watch": "concurrently -k -n SERVER,BUILD -c yellow,magenta \"nodemon server.js\" \"npm --prefix web run build -- --watch\"",
    "deploy": "npm run build && npm start"
  },
  "dependencies": {
    "express": "^4.18.2",
    "redis": "^4.6.7",
    "axios": "^1.5.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1",
    "concurrently": "^8.2.2"
  }
}
