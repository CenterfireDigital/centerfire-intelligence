clients:
  claude_code:
    commands:
      - "*"  # Allow all commands initially
    tty: true
    sessions: true
  
  personal_agent:
    commands:
      - "*"  # Allow APOLLO to execute system commands
    tty: false
    sessions: false
  
  # Future client contracts can be added here
  # web_ui:
  #   commands:
  #     - "ls"
  #     - "git status"
  #   tty: false
  #   sessions: false